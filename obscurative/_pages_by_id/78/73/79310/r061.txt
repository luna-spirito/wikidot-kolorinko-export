---
title: "Интерактивный рассказ"
tags: []
page_id: "787379310"
site: "obscurative"
slug: "archive:game"
revision: 61
revision_id: "834011995"
author: 4518363
timestamp: 1557084777
---

[!--[[module CSS]]
#side-bar {
  display: none;
}
#main-content {
  margin: 0;
}
[[/module]]--]
[[html]]
<!-- CSS -->

<style>
#over {
  display: none;
  position: absolute;
  top: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  text-align: center;
}
#over-text {
  display: table-cell;
  vertical-align: middle;
}
#text {
  height: 200px;
  width: 100%;
  border: solid black 1px;
  box-sizing: border-box;
}
</style>

<!-- HTML -->

<div>
  <div id="over">
    <div id="over-text">
      Hello.
    </div>
  </div>
  <div id="content">
    <div id="text">
    </div>
    <div id="buttons">
    </div>
  </div>
</div>

<!-- JS -->
"use strict";
<script>
var text = document.getElementById("text");
var buttons = document.getElementById("buttons");
var url = "http://university-of-pandora.wdfiles.com/local--files/draft%3Agame/";
var loc = {"description": 3};
function log(msg) {
  text.innerHTML += msg+"\n";
}
function talk(dialog) {
  let speaker = "";
  if(typeof dialog === 'string')
    dialog = [dialog];
  for(let i = 0; i < dialog.length; i++) {
    let val = dialog[i];
    if(typeof val === 'object') {
      speaker = val[0];
      val = val[1];
    }
    let msg = "";
    if(speaker != "")
      msg = speaker+": ";
    log(msg+val);
  }
}
function load(id) {
  let xhr;
  let timeout;
  function post() {
    xhr = new XMLHttpRequest()
    xhr.open("GET", url+id+".json");
    xhr.addEventListener("loadend", end);
    xhr.send();
    timeout = setTimeout(abort, 1500);
  }
  function end() {
    clearTimeout(timeout);
    if(xhr.status == 200) {
      loc = JSON.parse(xhr.responseText);
      talk(loc.description);
    } else {
      setTimeout(post, 500);
    }
  }
  function abort() {
    xhr.abort();
    setTimeout(post, 500);
  }
  post();
}

load("start");
</script>
[[/html]]

